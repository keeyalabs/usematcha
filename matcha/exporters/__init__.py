# SPDX-License-Identifier: Apache-2.0
"""matcha.exporters — structured output surfaces (JSONL, Prometheus, OTLP).

Internal subpackage; the stable user-facing surface is the CLI flags
(`--output`, `--prometheus`, `--otlp`) and the JSONL record schema
documented in ARCHITECTURE.md.
"""
