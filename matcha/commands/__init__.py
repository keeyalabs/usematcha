# SPDX-License-Identifier: Apache-2.0
"""matcha.commands — CLI-support modules (diff, monitor, stdout metric extraction).

Internal subpackage; the stable surface is the `matcha diff` / `matcha monitor`
CLI subcommands. Direct imports of these modules are not part of the public API.
"""
